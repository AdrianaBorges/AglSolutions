import { CadastroTabDirective } from './cadastro-tab.directive';

describe('CadastroTabDirective', () => {
  it('should create an instance', () => {
    const directive = new CadastroTabDirective(null);
    expect(directive).toBeTruthy();
  });
});
