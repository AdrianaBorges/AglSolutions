import { GridPesquisaDetalheTemplateDirective } from './grid-pesquisa-detalhe-template.directive';

describe('GridPesquisaDetalheTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new GridPesquisaDetalheTemplateDirective(null);
    expect(directive).toBeTruthy();
  });
});
