import { MenuSistemaModel } from './menu-sistema.model';

describe('MenuSistema.Model', () => {
  it('should create an instance', () => {
    expect(new MenuSistemaModel(null, null)).toBeTruthy();
  });
});
